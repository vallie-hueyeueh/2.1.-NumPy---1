"""
ЛЕКЦИЯ 10: БИБЛИОТЕКИ PYTHON ДЛЯ НАУЧНЫХ РАСЧЕТОВ И ВИЗУАЛИЗАЦИИ

"""

import numpy as np                     # Для математики и генерации чисел
import pandas as pd                    # Для работы с таблицами
import matplotlib.pyplot as plt        # Главный инструмент для рисования графиков
import matplotlib.image as mpimg       # Для загрузки картинок

"""
=============================================================================
РАЗДЕЛ 1: РАБОТА С ИЗОБРАЖЕНИЯМИ
=============================================================================
"""
# img = mpimg.imread('ttt.png') # Загружаем картинку (файл ttt.png должен быть в папке)
# plt.imshow(img)               # Показываем ее
# plt.axis('off')               # Скрываем оси с цифрами (нам нужна просто картинка)
# plt.show()                    # Команда, которая выводит окно на экран

"""
=============================================================================
РАЗДЕЛ 2: ПРОСТЫЕ ГРАФИКИ (ПОДХОД MATLAB)
Создаем два графика друг под другом с помощью plt.subplot(строки, колонки, номер)
=============================================================================
"""
# x = np.linspace(0, 10, 100)   # Создаем 100 точек от 0 до 10 для оси X

# plt.subplot(2, 1, 1)          # Сетка из 2 строк и 1 колонки. Выбираем 1-й график (верхний)
# plt.plot(x, np.sin(x))        # Рисуем синусоиду

# plt.subplot(2, 1, 2)          # Выбираем 2-й график (нижний)
# plt.plot(x, np.cos(x))        # Рисуем косинусоиду
# plt.show()

"""
=============================================================================
РАЗДЕЛ 3: ОБЪЕКТНО-ОРИЕНТИРОВАННЫЙ ПОДХОД
Более современный способ рисовать несколько графиков через fig и ax.
fig - это окно (полотно), ax - это список самих графиков внутри окна.
=============================================================================
"""
# fig, ax = plt.subplots(2)     # Сразу создаем 2 графика друг под другом
# ax[0].plot(x, np.sin(x))      # Обращаемся к верхнему графику по индексу 0
# ax[1].plot(x, np.cos(x))      # Обращаемся к нижнему графику по индексу 1
# plt.show()


"""
=============================================================================
РАЗДЕЛ 4: ЦВЕТА И СТИЛИ ЛИНИЙ
Как раскрасить линии и сделать их пунктирными.
=============================================================================
"""
# x = np.linspace(0, 20, 1000)
# ax = plt.gca() # Получить текущие оси (Get Current Axes)

# ax.plot(x, np.sin(x), color='blue')                                  # По названию
# ax.plot(x, np.sin(x - 1), color='g', linestyle='solid')              # Кратко (g = green), сплошная
# ax.plot(x, np.sin(x - 2), color='0.75', linestyle='dashed')          # Градации серого (0.75), пунктир
# ax.plot(x, np.sin(x - 3), color='#FFDDAA', linestyle='dashdot')      # HEX-код цвета, штрихпунктир
# ax.plot(x, np.sin(x - 4), color=(1.0, 0.2, 0.3), linestyle='dotted') # Кортеж RGB, точечная линия
# ax.plot(x, np.sin(x - 5), color="salmon")                            # Еще одно название

# plt.show()


"""
=============================================================================
РАЗДЕЛ 5: НАСТРОЙКА ОСЕЙ И МАСШТАБА
Как обрезать график или изменить его масштаб.
=============================================================================
"""
# fig, ax = plt.subplots(5, figsize=(8, 10)) # 5 графиков по вертикали

# ax[0].plot(x, np.sin(x)) # Обычный график
# ax[1].plot(x, np.sin(x)) 
# ax[1].set_xlim(-2, 12)   # Жестко задаем границы по X
# ax[1].set_ylim(-1.5, 1.5) # Жестко задаем границы по Y

# ax[2].plot(x, np.sin(x))
# ax[2].set_xlim(12, -2)   # Если перевернуть границы, график отзеркалится

# ax[3].plot(x, np.sin(x))
# ax[3].autoscale(tight=True) # Убирает пустые поля по краям (вплотную)

# ax[4].plot(x, np.sin(x))
# ax[4].axis('equal')      # Делает масштаб X и Y одинаковым (один шаг по X равен шагу по Y)

# plt.show()


"""
=============================================================================
РАЗДЕЛ 6: ПОДПИСИ, ЗАГОЛОВКИ И ЛЕГЕНДЫ
=============================================================================
"""
# plt.subplots_adjust(hspace=0.5) # Увеличиваем отступ между графиками

# plt.subplot(3, 1, 1)
# plt.plot(x, np.sin(x))
# plt.title("Синус")
# plt.xlabel("Ось X")
# plt.ylabel(r"sin(x) $\sqrt{2}$") # r"" позволяет писать математические формулы красиво

# plt.subplot(3, 1, 2)
# plt.plot(x, np.sin(x), '-g', label='Линия Синуса') # label нужно для легенды
# plt.plot(x, np.cos(x), 'b', label='Линия Косинуса')
# plt.title("Синус и Косинус")
# plt.legend() # Выводит окошко с подсказками (что значит каждый цвет)

# plt.subplot(3, 1, 3)
# plt.plot(x, np.sin(x), '-k', label=r'$\sin(x)$') # -k означает сплошная черная линия
# plt.plot(x, np.cos(x), '-r', label=r'$\cos(x)$') # -r означает сплошная красная линия
# plt.title("Синус и Косинус 2")
# plt.legend()

# plt.show()


"""
=============================================================================
РАЗДЕЛ 7: ДИАГРАММЫ РАССЕЯНИЯ (SCATTER PLOT)
Рисуем точки вместо линий. Меняем размер и цвет каждой точки.
=============================================================================
"""
# x = np.linspace(0, 10, 30)
# y = np.sin(x)

# # Способ 1: Использовать обычный plot, но с маркером "o" (кружок)
# # plt.plot(x, y, "-o", markersize=15, markerfacecolor="red", markeredgecolor="blue", markeredgewidth=3, linewidth=4, color="black")

# # Способ 2: Настоящий scatter, где каждая точка может быть уникальной
# colors = rng.random(30)       # Генерируем 30 случайных цветов
# sizes = 1000 * rng.random(30) # Генерируем 30 случайных размеров

# plt.scatter(x, y, c=colors, s=sizes, alpha=0.5, cmap='viridis') 
# plt.colorbar() # Показать шкалу цветов сбоку
# plt.show()


"""
=============================================================================
РАЗДЕЛ 8: РЕАЛЬНЫЕ ДАННЫЕ (ИРИСЫ ФИШЕРА)
Анализ знаменитого датасета цветков Ириса.
Требует установленной библиотеки scikit-learn (pip install scikit-learn).
=============================================================================
"""
# from sklearn.datasets import load_iris
# iris = load_iris(as_frame=True) # Загружаем данные как таблицу

# plt.scatter(
#     iris.frame["sepal length (cm)"],          # Ось X: длина чашелистика
#     iris.frame["sepal width (cm)"],           # Ось Y: ширина чашелистика
#     s=100 * iris.frame["petal length (cm)"],  # Размер точки зависит от длины лепестка
#     c=iris.frame["target"],                   # Цвет зависит от вида цветка (target)
#     alpha=0.5,
#     cmap='plasma'
# )
# plt.xlabel("Длина чашелистика (cm)")
# plt.ylabel("Ширина чашелистика (cm)")
# plt.colorbar(label="Вид Ириса")
# plt.show()


"""
=============================================================================
РАЗДЕЛ 9: ГРАФИКИ С ПОГРЕШНОСТЯМИ (ERROR BARS)
Полезно для научных статей, где нужно показать неточность измерений
=============================================================================
"""
# x = np.linspace(0, 10, 100)
# dy = 0.5                               # Фиксированная погрешность (плюс-минус 0.5)
# y = np.sin(x) + dy * rng.random(100)   # Добавляем "шум" к синусоиде

# plt.plot(x, y, "-k")                   # Рисуем основную черную линию
# plt.errorbar(x, y, yerr=dy, fmt=".r")  # Добавляем красные "усики" погрешности

# # Закрашиваем область погрешности серым цветом для красоты
# plt.fill_between(x, y - dy, y + dy, color="0.75", alpha=0.3)
# plt.show()


"""
=============================================================================
РАЗДЕЛ 10: КОНТУРЫ И ТЕПЛОВЫЕ КАРТЫ
Визуализация трехмерной (3D) функции на плоском (2D) экране
=============================================================================
"""
# x = np.linspace(0, 5, 50)
# y = np.linspace(0, 5, 50)
# X, Y = np.meshgrid(x, y) # Создаем сетку координат

# def f(x, y): # Искусственная математическая "гористая" поверхность
#     return np.sin(x)**4 + np.cos(y + x * 30) * np.sin(y)

# Z = f(X, Y) # Вычисляем высоту (Z) для каждой точки сетки

# # plt.contour(X, Y, Z, colors="black")                # Простые контуры (как на картах высот)
# # plt.contourf(X, Y, Z, 10, cmap="RdGy")              # Закрашенные контуры (f = filled)

# # Отображаем сетку как изображение (тепловую карту)
# plt.imshow(
#     Z,
#     extent=[0, 5, 0, 5],
#     origin="lower",
#     cmap="RdGy",
#     interpolation="gaussian", # Размытие пикселей для плавности
#     aspect="equal",
#     alpha=0.8
# )
# plt.colorbar(label="Высота (Z)")
# plt.show()


"""
=============================================================================
РАЗДЕЛ 11: 1D и 2D ГИСТОГРАММЫ
Отображение плотности распределения данных
=============================================================================
"""
# # --- Обычные (1D) гистограммы ---
# x1 = rng.normal(0, 0.8, 1000)
# x2 = rng.normal(-2, 1, 1000)
# x3 = rng.normal(3, 2, 1000)

# # Словарь общих настроек (передадим их во все гистограммы через **p)
# p = dict(alpha=0.3, bins=40) 

# plt.hist(x1, **p, label="x1")
# plt.hist(x2, **p, label="x2")
# plt.hist(x3, **p, label="x3")
# plt.legend()
# plt.show()


# # --- Двумерные (2D) гистограммы ---
# mean = [0, 0]
# cov = [[1, 1], [1, 3]]
# # Генерируем "облако" из 10 000 точек
# x, y = rng.multivariate_normal(mean, cov, 10000).T

# # Строим двумерную гистограмму (где больше точек - цвет ярче)
# plt.hist2d(x, y, bins=40, cmap='Blues') 

# # Альтернативный вид (шестиугольники):
# # plt.hexbin(x, y, gridsize=30, cmap='Blues')

# plt.colorbar(label="Плотность точек")
# plt.show()

# # Вывод распределения просто в виде чисел в консоль (без графиков)
# print("Числовое представление гистограммы:")
# print(np.histogram2d(x, y, bins=2))